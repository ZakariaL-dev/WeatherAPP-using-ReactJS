import { createContext } from "react";

export const CityNameContext = createContext("City Name");

export const TodayWeatherContext = createContext(null);

export const FivedaysWeatherContext = createContext(null);
